<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <style>
        body, html {
            background-color: #fff !important;
        }
        div {
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .tools {
            background-color: #333; 
            color: white; 
            width: 50px; 
            border-radius: 100px; 
            height: 50px; 
            font-size: 30px; 
            bottom: 20px; 
            right: 20px; 
            position: fixed
        }
        .tools a {
            text-decoration: none; 
            color: white
        }
        img {
            width: 100%;
        }
    </style>
  </head>
  <body>
    <div class="container">
        <img src="<%= locals.fetch(:diagram, 'Not found') %>">
    </div>
    <div class="tools">
       <a href="<%= locals.fetch(:source, 'Not found') %>" target="_blank">✎</a>
    </div>
  </body>
</html>